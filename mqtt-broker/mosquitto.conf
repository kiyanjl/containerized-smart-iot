# Listen on default MQTT port
listener 1883

# Allow anonymous connections (for development)
allow_anonymous true

# Disable persistence for simplicity (can be enabled later)
persistence false

# Logging
log_type all
